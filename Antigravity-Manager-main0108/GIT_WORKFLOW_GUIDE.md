# Git 多设备协作工作流程指南

本文档记录了如何在多台电脑之间同步代码，以及如何跟踪上游开源项目的更新。

---

## 🏗️ 架构关系图

```
                    ┌─────────────────────────────────────┐
                    │           远端仓库 (GitHub)          │
                    │                                     │
                    │  ┌─────────────────────────────┐    │
                    │  │  upstream (原开源项目)       │    │
                    │  │  lbjlaq/Antigravity-Manager │    │
                    │  └──────────────┬──────────────┘    │
                    │                 │ fork              │
                    │                 ▼                   │
                    │  ┌─────────────────────────────┐    │
                    │  │  origin (您的 Fork)          │    │
                    │  │  zhaoanran8666/Antigravity  │    │
                    │  └──────────────┬──────────────┘    │
                    └─────────────────┼───────────────────┘
                                      │
                    ┌─────────────────┼───────────────────┐
                    │                 │ push/pull         │
                    │     ┌───────────┴───────────┐       │
                    │     ▼                       ▼       │
                    │  ┌──────────┐         ┌──────────┐  │
                    │  │ Windows  │         │   Mac    │  │
                    │  │   电脑   │         │   电脑   │  │
                    │  │  (main)  │         │  (main)  │  │
                    │  └──────────┘         └──────────┘  │
                    │              本地设备                │
                    └─────────────────────────────────────┘
```

---

## 📋 远程仓库配置

| 名称 | 地址 | 用途 |
|------|------|------|
| `origin` | `https://github.com/zhaoanran8666/Antigravity-Manager.git` | 您的 Fork，用于推送修改 |
| `upstream` | `https://github.com/lbjlaq/Antigravity-Manager.git` | 原开源项目，用于拉取更新 |

---

## 🔄 日常工作流程速查表

| 场景 | 命令 |
|------|------|
| 原仓库有更新 → 同步到本地 | `git fetch upstream` → `git merge upstream/main` |
| 本地有修改 → 推送到远端 | `git push origin main` |
| 另一台电脑 → 获取最新代码 | `git pull origin main` |
| 有冲突时 | 手动解决 → `git add .` → `git commit` |

---

## 📖 详细场景说明

### 场景1：原开源项目 (upstream) 有更新

当原作者发布了新版本或修复了 bug，您想要同步这些更新：

```bash
# 第一步：获取上游最新代码
git fetch upstream

# 第二步：合并上游更新到本地 main 分支
git merge upstream/main

# 第三步（如有冲突）：解决冲突后
git add .
git commit -m "merge: 合并上游更新"

# 第四步：推送到您的 Fork，让另一台电脑也能获取
git push origin main
```

### 场景2：Windows 电脑修改后同步到 Mac

**在 Windows 电脑上：**

```bash
# 提交本地修改
git add .
git commit -m "描述您的修改"

# 推送到您的 Fork
git push origin main
```

**在 Mac 电脑上：**

```bash
# 拉取最新代码
git pull origin main
```

### 场景3：Mac 电脑修改后同步到 Windows

**在 Mac 电脑上：**

```bash
# 提交本地修改
git add .
git commit -m "描述您的修改"

# 推送到您的 Fork
git push origin main
```

**在 Windows 电脑上：**

```bash
# 拉取最新代码
git pull origin main
```

### 场景4：Mac 电脑首次设置（克隆项目）

```bash
# 克隆您的 Fork（不是原项目）
git clone https://github.com/zhaoanran8666/Antigravity-Manager.git

# 进入项目目录
cd Antigravity-Manager

# 添加上游仓库
git remote add upstream https://github.com/lbjlaq/Antigravity-Manager.git

# 验证远程仓库配置
git remote -v
```

---

## ⚠️ 重要注意事项

### 1. 先 pull 后 push
每次开始工作前，先执行：
```bash
git pull origin main
```
这样可以确保您的本地代码是最新的，避免冲突。

### 2. 避免同时修改
- 不要在两台电脑上同时修改同一个文件
- 如果必须同时工作，使用不同的分支

### 3. 冲突处理
如果 `git pull` 或 `git merge` 时出现冲突：
```bash
# 1. Git 会标记冲突文件，手动编辑解决冲突
# 2. 解决后添加文件
git add <冲突文件>

# 3. 提交合并
git commit -m "resolve: 解决合并冲突"

# 4. 推送
git push origin main
```

### 4. 功能分支（可选）
如果您在做大功能开发，建议创建功能分支：
```bash
# 创建并切换到新分支
git checkout -b feature/my-new-feature

# 开发完成后合并回 main
git checkout main
git merge feature/my-new-feature

# 删除功能分支（可选）
git branch -d feature/my-new-feature
```

---

## 🔧 常用命令参考

```bash
# 查看远程仓库配置
git remote -v

# 查看所有分支（包括远程）
git branch -a

# 查看当前状态
git status

# 查看提交历史
git log --oneline -10

# 查看文件修改差异
git diff

# 暂存所有修改
git stash

# 恢复暂存的修改
git stash pop
```

---

## 📅 更新日期

本文档创建于：2026-01-13
